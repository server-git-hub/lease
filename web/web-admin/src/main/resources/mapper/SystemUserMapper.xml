<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.lease.web.admin.mapper.SystemUserMapper">

    <resultMap id="rMap" type="com.atguigu.lease.web.admin.vo.system.user.SystemUserItemVo">
        <id column="su_id" property="id"/>
        <result column="su_name" property="name"/>
        <result column="sp_name" property="postName"/>
    </resultMap>


    <select id="customPage" resultMap="rMap">
        select
            su.id su_id,su.username,su.name su_name,su.type,su.phone,su.avatar_url,su.additional_info,
            su.post_id,su.status,sp.name sp_name
        from system_user su left join system_post sp
                                      on su.post_id=sp.id and sp.is_deleted=0
        where su.is_deleted=0
        <if test="queryVo.name">
            and su.name like concat('%',#{queryVo.name},'%')
        </if>
        <if test="queryVo.phone">
            and su.phone like concat('%',#{queryVo.phone},'%')
        </if>
    </select>

    <select id="customById" resultMap="rMap">
        select
        su.id su_id,su.username,su.name su_name,su.type,su.phone,su.avatar_url,su.additional_info,
        su.post_id,su.status,sp.name sp_name
        from system_user su left join system_post sp
        on su.post_id=sp.id and sp.is_deleted=0
        where su.is_deleted=0 and su.id=#{id};
    </select>

</mapper>
